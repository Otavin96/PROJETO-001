import { ToolsRepository } from '@/tools/repositories/tools.repository'
import { CreateToolUseCase } from './create-tool.usecase'

describe('CreateToolUsecase unit Tests', () => {
  let sut: CreateToolUseCase.UseCase
  let repository: ToolsRepository

  beforeEach(() => {})
})
